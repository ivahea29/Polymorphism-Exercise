namespace polyAndInheriatanceTask
{
    public partial class Form1 : Form
    {
        public Form1()
        {
            InitializeComponent();
        }

        // Write method to read the input file and create Array[A]

        private double toProf(Animals[] A)
        {
            double totProf = 0.0; // Calculating Profibility
            for (int i=0; i < 10; i++)
            {
                //tot = tot + A[i].getProf();
            }
            return (2.2);
        }
        private void button1_Click(object sender, EventArgs e)
        {
            // This is where the program starts
            Animals[] A = new Animals[10]; // Only pointers
        }
    }
}